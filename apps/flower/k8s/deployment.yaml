apiVersion: apps/v1
kind: Deployment
metadata:
  name: flower-service
  labels:
    app: flower-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flower-service
  template:
    metadata:
      labels:
        app: flower-service
    spec:
      containers:
        - name: flower-service
          image: flower-service:latest
          ports:
            - containerPort: 3004
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 5
            periodSeconds: 5
